name: Build and Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 6.0.x
    - name: Restore dependencies
      run: dotnet restore platform/windows/CatsUp.Windows.csproj
    - name: Build
      run: dotnet build platform/windows/CatsUp.Windows.csproj --no-restore
    - name: Test
      run: dotnet test tests/CatsUp.Tests.csproj --no-build --verbosity normal

  build-macos:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4
    - name: Build
      working-directory: platform/macos
      run: swift build -v
    - name: Test
      working-directory: platform/macos
      run: swift test -v

  build-linux:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    - name: Install dependencies
      working-directory: platform/linux
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest
    - name: Test
      run: |
        export PYTHONPATH=$PYTHONPATH:$(pwd)
        pytest tests/test_linux.py

  build-ui:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
    - name: Install dependencies
      working-directory: ui
      run: npm ci
    - name: Build
      working-directory: ui
      run: npm run build
